% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cache_handler.R
\name{nxt_cache_handler}
\alias{nxt_cache_handler}
\title{Drop-in cache handler for the nordstat family}
\usage{
nxt_cache_handler(
  source,
  entity,
  cache,
  cache_location,
  key_params = list(),
  kind = c("data", "metadata"),
  ttl_days = NXT_DEFAULT_TTL_DAYS,
  reconstruct_extra = list(),
  normalize_extra = list()
)
}
\arguments{
\item{source}{One of \code{"kolada"}, \code{"trafa"}, \code{"pixieweb"}.}

\item{entity}{Entity keyword (e.g. \code{"values"}, \code{"products"}, \code{"tables"}).}

\item{cache}{Logical — whether caching is enabled.}

\item{cache_location}{Either an \code{nxt_handle} from \code{\link[=nxt_open]{nxt_open()}} or a path /
function-returning-a-path to a \code{.sqlite} file.}

\item{key_params}{Named list of parameters that together with \code{source} +
\code{entity} form a unique cache key.}

\item{kind}{Either \code{"data"} (default, cell-level) or \code{"metadata"}
(whole-BLOB). Determines the storage/TTL strategy.}

\item{ttl_days}{Time-to-live in days. Defaults to 30.}

\item{reconstruct_extra}{Named list of extra arguments forwarded to the
reconstruct function — used when trafa/pixieweb need product/alias/etc.
at load time. Ignored when \code{kind = "metadata"}.}

\item{normalize_extra}{Same for the normalize function, used at store time.
Ignored when \code{kind = "metadata"} — metadata is serialized whole.}
}
\value{
A function with signature \verb{function(method, df = NULL)}.
}
\description{
Produces a closure matching the \code{cache_handler()} contract used by
rKolada, rTrafa, and pixieweb: a function of \verb{(method, df)} where \code{method}
is one of \code{"discover"}, \code{"load"}, or \code{"store"}. Unlike the per-package
\code{.rds} handlers, this one stores values in a shared SQLite file. Two kinds
of cached content are supported:
}
\details{
\itemize{
\item \code{kind = "data"} (default) — statistical data stored at cell granularity
with cross-query freshness. Matches the existing rKolada \code{get_values()}
/ rTrafa \code{get_data()} / pixieweb \code{get_data()} integration.
\item \code{kind = "metadata"} — whole serialized objects (tibbles or lists) stored
as BLOBs on the \code{queries} row. Matches the metadata-caching integration
for \code{get_kpi()}, \code{get_products()}, \code{get_tables()}, etc. TTL is query-level.
}
}
