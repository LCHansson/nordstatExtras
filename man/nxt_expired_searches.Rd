% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recorded_search.R
\name{nxt_expired_searches}
\alias{nxt_expired_searches}
\title{List recorded searches that are due for a refresh}
\usage{
nxt_expired_searches(handle, max_age = 30L)
}
\arguments{
\item{handle}{\verb{<nxt_handle>} from \code{\link[=nxt_open]{nxt_open()}}.}

\item{max_age}{Default maximum age in days when a row's own
\code{ttl_days} is NULL or smaller. Default 30.}
}
\value{
A tibble with columns \code{query}, \code{source}, \code{last_run_at}
(POSIXct), \code{ttl_days}, \code{last_hit_count}. Ordered oldest-first.
}
\description{
Returns the \verb{(query, source)} pairs whose \code{last_run_at} is older than
\code{max_age} days (or their own \code{ttl_days} entry, whichever is smaller).
A cron job can iterate the result and re-run each search against the
live API, passing the fresh entity list back to \code{\link[=nxt_record_search]{nxt_record_search()}}.
}
