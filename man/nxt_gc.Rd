% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/maintenance.R
\name{nxt_gc}
\alias{nxt_gc}
\title{Garbage-collect stale cache rows}
\usage{
nxt_gc(handle, max_age_days = NXT_DEFAULT_TTL_DAYS)
}
\arguments{
\item{handle}{An \code{nxt_handle}.}

\item{max_age_days}{Maximum age in days. Default 30.}
}
\value{
\code{invisible(NULL)}.
}
\description{
Branches on \code{queries.kind}:
}
\details{
\itemize{
\item \code{kind='data'}: deletes individual cells older than \code{max_age_days}, drops
junction rows pointing at them, then removes queries that lost every
cell. Queries that still have at least one fresh cell are kept.
\item \code{kind='metadata'}: deletes queries whose \code{fetched_at} is older than
\code{max_age_days} (query-level TTL — metadata blobs are opaque and can't
benefit from cross-query freshness).
}

Finally prunes orphan rows from \code{cell_dims} and \code{meta_search}.
}
\examples{
\donttest{
path <- tempfile(fileext = ".sqlite")
handle <- nxt_open(path)

# Remove cells and metadata older than 30 days (default)
nxt_gc(handle)

# Custom TTL: remove anything older than 7 days
nxt_gc(handle, max_age_days = 7)

nxt_close(handle)
unlink(path)
}
}
