% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/recorded_search.R
\name{nxt_record_search}
\alias{nxt_record_search}
\title{Record a live search so the local index finds it next time}
\usage{
nxt_record_search(handle, query, source, entity_ids, ttl_days = 30L)
}
\arguments{
\item{handle}{\verb{<nxt_handle>} from \code{\link[=nxt_open]{nxt_open()}}.}

\item{query}{Character scalar — the user's search term (kept verbatim
in \code{recorded_searches}; added as a token to \code{search_keywords}).}

\item{source}{Character scalar — one of \code{"kolada"}, \code{"pixieweb"},
\code{"trafa"}.}

\item{entity_ids}{Character vector of entity ids to associate with the
query (from the respective \verb{get_*} output's \code{id}/\code{name} column).}

\item{ttl_days}{Integer — max age before \code{\link[=nxt_expired_searches]{nxt_expired_searches()}}
returns this search for re-running. Default 30.}
}
\value{
Invisibly, the number of meta_search rows updated.
}
\description{
Appends \code{query} to the \code{search_keywords} column of every entity in
\code{entity_ids} (deduplicated, whitespace-separated), refreshes the FTS5
index, and UPSERTs the query into \code{recorded_searches} with a \code{last_run_at}
timestamp. The entities must already exist in \code{meta_search} — call the
source-specific \verb{get_*} functions with the nordstatExtras cache first.
}
