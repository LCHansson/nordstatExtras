% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/search.R
\name{nxt_search}
\alias{nxt_search}
\title{Full-text search across cached metadata}
\usage{
nxt_search(handle, query, sources = NULL, entity_types = NULL, limit = 50L)
}
\arguments{
\item{handle}{An \code{nxt_handle} from \code{\link[=nxt_open]{nxt_open()}}.}

\item{query}{FTS5 query string. Supports prefix match via \verb{term*},
phrase match via \code{"exact phrase"}, and boolean operators \code{AND}/\code{OR}.}

\item{sources}{Optional character vector restricting results to
specific sources (e.g. \code{c("kolada", "pixieweb")}).}

\item{entity_types}{Optional character vector restricting results to
specific entity types (e.g. \code{c("kpi", "tables")}).}

\item{limit}{Maximum number of results to return. Default 50.}
}
\value{
A tibble with columns \code{source}, \code{entity_type}, \code{entity_id},
\code{title}, \code{description}, and \code{rank} (lower = better match). Empty
tibble if nothing matches.
}
\description{
Runs a typeahead-suitable search against the \code{meta_search} FTS5 index.
Every call to a metadata-caching function (e.g. \code{get_kpi()},
\code{get_products()}, \code{get_tables()}) populates this index as a side effect,
so \code{nxt_search()} returns matches across all three source packages in a
single query.
}
\examples{
\donttest{
path <- tempfile(fileext = ".sqlite")
handle <- nxt_open(path)

# Populate the search index by storing some metadata
ch <- nxt_cache_handler(
  source = "kolada", entity = "kpi", cache = TRUE,
  cache_location = handle, kind = "metadata",
  key_params = list()
)
ch("store", data.frame(
  id = c("N03700", "N01951"),
  title = c("Befolkning totalt", "Bruttoregionprodukt"),
  description = c("Antal invanare", "BRP per capita")
))

# Typeahead search
nxt_search(handle, "bef*")

# Filter by source
nxt_search(handle, "bef*", sources = "kolada")

nxt_close(handle)
unlink(path)
}

}
